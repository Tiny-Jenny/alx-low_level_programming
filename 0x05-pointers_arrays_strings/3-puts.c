#include "main.h"
/**
 * puts - prints a string, followedd by a new line, tostdout
 * @str: string to print
 */
void _puts (char *s)
	while (*str != '\0')
{
	while (str != '\0')
	{
		_putchar(*str++);
	}
		_putchar('\n*);
}
